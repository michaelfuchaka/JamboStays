{
  "name": "jambo-stays",
  "version": "1.0.0",
  "description": "JamboStays - Property rental platform",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}